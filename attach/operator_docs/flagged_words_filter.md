标记词比例过滤（`flagged_words_filter`）

**使用场景**
- 内容审核: 过滤包含敏感词的文本
- 数据清洗: 去除低质量或不适当的内容
- 合规性检查: 确保数据符合内容政策

**示例**
- 输入文本: `"基于前一步结果，除掉骂人、脏字等污秽数据和敏感词"`
- 配置: `lang='zh', max_ratio=0.045`
- 如果敏感词比例 > 4.5%，样本被过滤

- 输入：带有敏感词/黑名单词的文本集合。
- 输出：标记词占比低于设定阈值的样本。
- 核心：根据预设标记词表，统计每条样本中这些词的比例，剔除比例过高的样本。