特殊后缀过滤（`suffix_filter`）

**使用场景**
- 文件类型过滤: 只保留特定类型的文件
- 数据筛选: 根据文件格式筛选数据
- 格式验证: 确保数据符合预期格式

**示例**
- 输入数据: `{"path": "/data/file.txt", "text": "..."}`
- 配置: `suffixes=['.txt', '.md']`
- 文件后缀为.txt，在允许列表中，样本被保留

- 输入：带有文件名或字段后缀的样本。
- 输出：只保留后缀在白名单（如 `.py`、`.md`）内的样本。
- 核心：基于字段后缀做白名单过滤，常用于代码/文件数据集。