后缀过滤（`suffix_filter`）

**使用场景**
- 文件类型过滤: 只保留特定类型的文件
- 数据筛选: 根据文件格式筛选数据
- 格式验证: 确保数据符合预期格式

**示例**
- 输入数据: `{"path": "/data/file.txt", "text": "..."}`
- 配置: `suffixes=['.txt', '.md']`
- 输出: 文件后缀为.txt，在允许列表中，样本被保留