多轮对话去重并保存（`dedup_and_save_deduplicator`）
- 输入：已通过 `encode_and_get_nearest_mapper` 查找到相似集合的多轮对话数据。
- 输出：每个相似集合只保留一条代表样本，按任务类型拆分写入不同文件。
- 核心：
  - 对每个相似集合保留索引最小的样本，其余视为重复删掉。
  - 根据任务类型（如问答、纠错、推荐等）将最终样本分桶保存，方便后续按任务训练。
